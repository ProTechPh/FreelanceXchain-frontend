import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact Us | FreelanceXchain',
  description: 'Get in touch with FreelanceXchain support for escrow, contracts, verification, and account issues.',
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
